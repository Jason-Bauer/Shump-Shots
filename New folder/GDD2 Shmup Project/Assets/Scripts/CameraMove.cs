using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraMove : MonoBehaviour {

    //public Camera mainCamera;
    //float viewPosX = 0.25f;
    //float viewPosy = 0.0f;
    //float viewWidth = 0.5f;
    //float viewHeight = 1.0f;
    //public float timer;
    //public GameObject background;
    //public Material backmat;
    //public Material newback;
    //public Material warpback;


    // Use this for initialization
    void Start () {
        //viewPosX = 0.25f;
        //viewPosy = 0.0f;
        //viewWidth = 1f;
        //viewHeight = 4.0f;
        //timer = 0.0f;
        ////mainCamera.enabled = true;
        //mainCamera.orthographic = true;
        //mainCamera.orthographicSize = 5.0f;
        //mainCamera.rect = new Rect(viewPosX, viewPosy, viewWidth, viewHeight);
        //backmat = background.GetComponentInChildren<Renderer>().material;
	}
	
	// Update is called once per frame
	void Update () {
        //timer += .01f;
        
        //Vector3 position = background.transform.position;
        //position.x -= 0.025f;

        //if (backmat != newback)
        //{
        //    if (position.x < -2)
        //    {
        //        position.x = -1;
        //    }
        //}
        //else
        //{
        //    if (position.x < -15)
        //    {
        //        position.x = -.5f;
        //    }
        //}
        //    background.transform.position = position;
        //if (timer > 8.0f)
        //{
        //    background.GetComponentInChildren<Renderer>().material = newback;
        //    backmat = newback;
        //}

        //else if (timer>7.5f)
        //{
            
        //    background.GetComponentInChildren<Renderer>().material=warpback;
        //    backmat = warpback;
        //}
        //if (timer > 7f)
        //{
            //if (GetComponent<AudioSource>().isPlaying != true)
            //{
            //    GetComponent<AudioSource>().Play();
            //}
          
        //}
    }
}
